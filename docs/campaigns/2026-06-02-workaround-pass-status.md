# Scale-test campaign status

_Running log of the 2026-06-02/03 workaround pass. KUBECONFIG=/tmp/mesh-verify-kubeconfig.yaml; contexts rosa-002…rosa-011 (10 spokes); mesh sizes 1→10._

## Stage A — istiod fanout harness
DONE — PR #29 merged (7-agent cycle, 2 rounds to consensus, CI green, PL28/PL29 added).

## Stage B — pin istiod 5 + resources
DONE — PR #30 merged; Argo synced all 10 spokes. Verified: CR `auto=false, reps=5, req 1/2Gi, lim 8Gi`; istiod 5/5; HPA gone; clusterz 10/10; idle ~10–17m CPU / ~950–1100Mi mem per spoke.

## Stage C — campaign

### Preflight + dry-run — DONE
Tools present; 10 spokes clean (no leftover *-test ns); flags reconciled. Dry-run matrices validated (no clusters touched):
| Suite | Matrix |
|---|---|
| propagation | 100 (10×10, --force-large-matrix) |
| churn | 10 |
| controlplane | 30 (10×3 scopings) |
| dataplane | 40 (10×4 QPS) |
| churn-dataplane | 30 (10×3 rates) |

### Live execution (serial)
Launch mode: **live smoke first (propagation), then the rest if clean.**

| # | Suite | Status | Result dir / notes |
|---|-------|--------|--------------------|
| 1 | propagation | ✅ DONE (smoke PASS) | sweep-20260602T222843Z; 455 OK / 5 TIMEOUT_P3 (98.9%); P1/P2 flat ~2s across mesh 1→10; fanout validated at full scale; ~2h06m wall-clock (O1 inflated); all ns clean |
| 2 | churn | 🔧 FIXED + re-running 8–10 | sizes 1–7 good (sweep …T010022Z). ROOT CAUSE: bare `scrape_ctx` calls (002:467/478/548/586) propagate `fanout_scrape_all`'s by-design non-zero on an incomplete scrape → `set -e` abort before the next line tags it. Fix: `|| true` ×4 + ERR trap (PR #31). Re-running 8–10 (sweep …T030239Z) from the fix branch; merge with 1–7 at analysis. Monitor PID-based now (pgrep self-match bug fixed). |

### AUTONOMOUS OVERNIGHT RUN (user asleep ~2026-06-03 03:00Z)
Mandate: drive churn 8–10 → controlplane → dataplane → churn-dataplane. Note non-blocking issues + subagent-investigate; investigate+fix breaking errors before retry; don't idle (silent-failure risk).
- **Churn fix CONFIRMED**: PR #31's `|| true`+ERR-trap works — size 8 cleared on re-run (had died there). Churn 8–10 finishing (sweep …T030239Z).
- **Pre-flight audit DONE**: controlplane/dataplane/churn-dataplane are SAFE from the bare-scrape `set -e` bug — no bare `fanout_scrape_all`/`scrape_ctx` calls; churn-dataplane captures the scrape in an assignment (`X="$(fanout_scrape_all …)"`, swallows the return). No preemptive fix needed.
- **Launch pattern for remaining suites**: nohup + PID file + robust monitor (kill -0 PID OR log-stall ≥600s → emit), to catch BOTH crash and hang (the churn monitor missed the crash via a pgrep self-match — fixed).
- Run order/params: controlplane `--mesh-sizes 1..10 --sidecar-scopings none,namespace,explicit` (matrix 30); dataplane `--qps-levels 10,100,500,1000` (40); churn-dataplane `--churn-rates 1,5,10` (30). Contexts rosa-002..011. Run from branch `fix/churn-set-e-scrape-abort` (only churn 002 differs from main).

### Follow-up branches (post-campaign /scale-test-review; NOT in campaign data)
- **PR #31** — churn set-e scrape-abort fix (the blocker; merge before others).
- **O1+O3**: `worktree-agent-a723a19ad6028f16e` @ `1971730` — pre-warm-backer P3 + `FANOUT_MAX_SKEW_MS` + PL30. 67/67 bats. Rebase on #31.
- **O4**: `worktree-agent-a7246af3908dd61f7` @ `55bc214` — churn EDS-convergence split (TSV 20→21 col). Rebase on #31.
- Churn root-cause investigation: DONE (its `(( )) && assign` hypothesis disproven empirically; real cause = bare `scrape_ctx`).
| 3 | controlplane | RUNNING (split, 2 fixes) | mesh 1–6 done in run …T035342Z (reduced 10×2 per O5). Run1 (10×3) crashed combo1 = O5 capacity. Run2 (10×2) hung combo19/mesh7 = PF readiness curl no --max-time → ~10min hang then die (PR #32 fix: --max-time 2 + restart stuck PF, VALIDATED — mesh7 PF now clears). Run3 …(controlplane3) re-running mesh 7–10 (10×2) on PF-fix branch. Merge 1–6 + 7–10 at analysis (both 10×2). |
| — | _open PRs_ | #31 churn set-e; #32 controlplane PF-retry. Both campaign hot-fixes, await review. |
| 4 | dataplane | ✅ DONE | …T094827Z; all 10 sizes, 100% pct_200; cross-cluster overhead flat ~1ms p50 mesh 1→10 (local ~2.3ms vs remote ~3.0ms), modest p99/max tail growth. |
| 5 | churn-dataplane | RUNNING (final) | reduced 3 deploys × scale-to 5 (~17 pods/cluster, O5); …churn-dataplane.log; robust monitor. NOTE per O6: 004 invokes probe bare (no wrap) — a combo failure could abort; monitor catches it, I retry remaining. |

### Post-campaign plan (user, morning 2026-06-03)
User will, AFTER this campaign completes: (1) review/merge the fix PRs (#31 churn set-e, #32 controlplane PF) + follow-up branches (O1+O3, O4); (2) **resize the cluster** (or lower istiod CPU request per O5); (3) **re-run the campaign clean** at full workload (controlplane 10×3, churn-dataplane 5×5). So THIS run is the workaround pass; the clean full-workload numbers come from the re-run. On churn-dataplane completion I produce a final campaign summary + a "what to re-run clean" list.

## Observations & issues (running log)

- **O1 — P3 propagation phase conflates workload startup (propagation suite).** P3 (remote watcher Envoy sees the canary endpoint) ran 8–102s with extreme, non-monotonic variance, while P1/P2 (control-plane push) were a steady ~2s. Almost certainly canary pod scheduling + image-pull/readiness dominating, not xDS propagation. P3 is watcher-Envoy-side (untouched by the fanout change) — pre-existing measurement caveat, not an aggregation bug. Follow-up: pre-warm/pre-create the canary so P3 measures propagation, not pod boot. P1/P2 are the trustworthy control-plane signal. **Fix designed** (confirmed via code: t0 at `002:1048` precedes a fresh canary Deployment create; P3 waits for `health_flags::healthy` = pod Ready; image `http-echo:latest`, no readinessProbe, sidecar inject). Recommended: pre-warm a backer pod in `001`, config-only label-flip at t0. Full proposal: `~/.claude/plans/p3-prewarm-proposal.md` → seed `/scale-test-review` as a follow-up after the campaign.
- **O2 — thin P1 histogram quantiles (propagation).** proxy_count=3 (source watcher + 2 gateways) since we ran `--watcher-replicas 1`. p1_conv_p50/p99 are low-sample; wall-clock P1 is fine. Bump `--watcher-replicas` (README wants ≥10/≥30 proxies) for robust per-push quantiles. Not a bug — a knob we left at default.
- **O3 — scrape-skew outlier to watch (propagation).** Through size 5: max scrape_skew mostly 200–300ms, but one 1263ms spike at size 3. Correctly surfaced by the `scrape_skew_ms` column. Watch whether it trends up at sizes 8–10 (peak ~50 port-forwards); if skew routinely exceeds a meaningful fraction of P1/P2 (~2s), the merged-snapshot coherence degrades and we'd want to stagger/cap the fanout scrape. **Materialized at size 10: one tick hit 4043ms skew** (~50 PFs) vs 100–350ms elsewhere — single outlier, row still OK, but confirms the stagger/cap follow-up is warranted at peak PF count. P1/P2 stayed flat ~2s across ALL sizes 1→10 regardless. **Fix designed** — root cause is a PL8 deviation: `fanout.sh:218` stamps the scrape timestamp at curl *completion*, not request *start*, so skew absorbs full curl-duration variance (4043ms ≈ the 5s scrape timeout ceiling under 50-PF contention). Recommended: `FANOUT_MAX_SKEW_MS` threshold-tag (default 1000ms) quarantining high-skew rows via existing `SCRAPE_INCOMPLETE` plumbing; keep completion-stamping (conservative coherence signal). Full proposal: `~/.claude/plans/o3-skew-proposal.md` → `/scale-test-review` follow-up. Also: 1 hard `TIMEOUT_P3` row so far — O1 manifesting as an actual timeout, not a fanout fault.
- **O4 — churn conv_remote conflates scale-up pod readiness with propagation (churn).** size 2: conv_local ~2.8s (clean control-plane) but conv_remote ~27s (consistent across iters 2–5; iter 1 = 7.7s). The remote watcher waits for the 20 newly-scaled-up source pods (5 deploys × 1→5) to become Ready before the endpoint count converges, so conv_remote ≈ source-pod scheduling/readiness + cross-cluster propagation, NOT pure xDS propagation. Same class as O1, but **arguably semi-legitimate for churn** (churn measures endpoint lifecycle, which includes pods coming up) — and notably *consistent* (~27s, node-capacity-bound), unlike P3's chaotic 8–100s. Decision needed: should churn's conv_remote isolate config-convergence from endpoint-readiness, or is endpoint-readiness part of the intended signal? Flagged for product call, not auto-fixed. conv_local is the clean control-plane number. **Options analyzed** — conv_remote stops on `health_flags::healthy` count (requires pod Ready+sidecar; image `busybox:stable` unpinned, no readinessProbe; scale-up only); code comment `002:443-445` claims "without gating on full pod rollout time" = half-true. Recommended **Split+Keep**: add a control-plane-only `convergence_remote_eds_ms` (P2 analogue, plumbing exists via `fanout_counter_by_label_sum ... type eds`) + relabel existing as reachability; pin image (Isolate) optional. Full proposal: `~/.claude/plans/o4-churn-convergence-proposal.md`. **Unifies with O1** as a "control-plane vs data-plane convergence" review bundle.
- **O5 — istiod CPU request grossly over-reserved → starves test workloads (BREAKING, campaign-wide).** Controlplane crashed on combo 1 (`dummy-svc-9 not Available within 300s`, `FailedScheduling: Insufficient cpu`). Root cause: Stage B pinned istiod to 5 replicas at **requests.cpu=1** (5 CPU/cluster reserved) but istiod uses ~3m idle / tens-of-m under load. ROSA nodes are small (4×3500m≈14 CPU/cluster), so 5 CPU of phantom istiod reservation + system + gateways leaves room for only ~24 sidecar-injected pods; 10 svc×3 rep=30 didn't fit. **CPU request is measurement-neutral** (doesn't affect any metric, only scheduling). PROPER FIX (needs user approval — production re-roll): lower `charts/spoke-ossm/values.yaml` `pilot.resources.requests.cpu` 1→~250m (keep mem req 2Gi / limit 8Gi for OOM headroom); frees ~3.75 CPU/cluster; then re-run controlplane at full 10×3. AUTONOMOUS WORKAROUND APPLIED: re-ran controlplane at **10 svc × 2 rep = 20 pods** (fits). NOTE: churn-dataplane (up to ~25 churn pods + fortio/cluster) may hit the same wall — will reduce deployment-count/scale-to there if needed. Controlplane data is at reduced replicas=2; recommend full re-run after the request fix.
- **O6 — probe fault-tolerance hardening (systemic; validates operator thesis).** At scale a single probe-instance failure is near-certain, so suites must record-and-continue, not abort. Audit confirms the ROOT is orchestrator-level: **all 5 sweep orchestrators invoke the 002/003 probe BARE under `set -e`**, so the first failing combo aborts the whole sweep (`propagation/006:277`, `controlplane/003:428/460`, `dataplane/003:228`, `churn/003:186`, `churn-dataplane/004:308/329`). #31/#32 fixed two instances; the orchestrator wrap is the real fix. churn-dataplane `004:333` ALREADY has the wrap (for cleanup) — replicate to probes. Priorities P0 wrap-probe-in-orchestrators → P1 land #31 → P2 controlplane resolve_pods/PF die→degrade → P3 ERR trap in all 10 scripts → P4 curl `--max-time` + bare-non-zero lint rule. Reserve `die` for whole-run preconditions only. propagation 002 is the reference-good exemplar. Full proposal: `~/.claude/plans/o6-probe-fault-tolerance-proposal.md` → dedicated `/scale-test-review` hardening pass.
- **O7 — tier-2 graded data quality / partial-coverage metrics (deeper follow-on to O6).** Instead of tainting the WHOLE ROW on a sub-measurement failure, grade quality PER METRIC. GEM: in mixed suites the headline metric isn't scrape-derived (churn-dataplane/dataplane = fortio Δp99; churn conv_local/remote = syncz-polled), yet one failed istiod pod scrape discards it via POISONED_SCRAPE — N/A only the istiod-side columns instead. Metric-type policy: invariant gauge=FULL on any survivor; histogram=PARTIAL (k/M≥0.5 + ≥30 samples); summed counter=lower-bound w/ coverage≥0.8 (NO extrapolation); threshold denominator=DO-NOT-DEGRADE (propagation P1 is the ONLY such trap, already guarded correctly). Coverage model: `.coverage` sidecar + `fanout_metric_quality` helper + `pods_scraped=k/M` column + report 3-tier (n_full/n_partial/n_none). New PL (next free number). Full proposal: `~/.claude/plans/o7-graded-coverage-proposal.md`. Pairs with O6 as the fault-tolerance hardening pass.
- **Positive — fanout validated live.** ISTIOD_REPLICAS=5 recorded (no single-replica die), P2 (merged/summed remote metrics) tracks P1 tightly, proxy_count correctly sums-then-dedupes to 3 across 5 pods, scrape_skew ~20–50ms (coherent), restarted=0, all rows status=OK. The pin+fanout pipeline works against real 5-replica istiod.
