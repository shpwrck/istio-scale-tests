apiVersion: v2
name: hub-kubeconfig-from-argosecret
description: ExternalSecret that reads a Terraform-owned Argo CD cluster Secret (*-argocd-gitops-cluster-secret) and writes a kubectl kubeconfig Secret (hub-only; one ManagedCluster name per sync).
type: application
version: 0.2.0
