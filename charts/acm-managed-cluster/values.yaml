# Membership for ManagedClusterSetBinding + Placement (must match ACM_CLUSTER_SET / ManagedClusterSet in openshift-gitops chart).
clustersetName: istio-scale-tests

defaultLabels:
  cloud: auto-detect
  vendor: OpenShift

managedCluster:
  name: ""
  labels: {}
  clientConfigs: []

managedServiceAccount:
  create: false
  createAddon: false
  name: argocd-gitops
