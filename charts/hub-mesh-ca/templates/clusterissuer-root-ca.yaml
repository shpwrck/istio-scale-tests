{{- if and .Values.rootClusterIssuer.enabled .Values.rootCA.enabled }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.rootCA.clusterIssuerName | quote }}
spec:
  ca:
    secretName: {{ .Values.rootCA.secretName | quote }}
{{- end }}
