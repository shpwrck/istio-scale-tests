1. Wait for cert-manager: kubectl get certificate -n {{ .Values.namespace }}
2. Root must be Ready before intermediates: kubectl get clusterissuer {{ .Values.rootCA.clusterIssuerName }}
3. Map issued Secrets to Istio cacerts or use Istio-CSR (see chart README).
